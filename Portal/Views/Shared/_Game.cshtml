@model Game

<div class="col-sm-6">
    <div class="card">
        <div class="card-body">
            <h5 class="card-title">@Model.Name</h5>
            <h6 class="card-subtitle mb-2">@Model.Genre</h6>
            <p class="card-text">Voor volwassenen: @Model.IsOnlyForAdults</p>
        </div>
    </div>
</div>